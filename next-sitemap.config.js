/** @type {import('next-sitemap').IConfig} */
const config = {
    siteUrl: 'https://www.jirikocvara.cz', // URL vašeho webu
    generateRobotsTxt: true, // (volitelně) generování robots.txt
    sitemapSize: 5000, // Maximální počet adres URL na jednu sitemapu
  };
  
  module.exports = config;
  